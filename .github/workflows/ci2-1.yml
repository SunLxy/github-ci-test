name: test2-1

on: 
  push:
    branch:
      - v1

jobs:
  build:
    runs-on: ubuntu-18.04
    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-node@v2
      with:
        node-version: 14 

    # - run: npm i
